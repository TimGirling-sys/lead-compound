# Render.com deployment configuration
services:
  - type: web
    name: compound-evolution
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        generateValue: true
